spring.application.name=Nail-BeautySalon
spring.jpa.hibernate.ddl-auto=update

spring.datasource.url = jdbc:mysql://${MYSQL_HOST:localhost}:3306/nail-beautysalondb?createDatabaseIfNotExist=true
spring.datasource.username = ${MYSQL_USER:root}
spring.datasource.password = ${MYSQL_PASSWORD:Sosha299*}
spring.datasource.driver-class-name = com.mysql.cj.jdbc.Driver


spring.jpa.show-sql=true
server.servlet.context-path=/nail-beautySalon
server.port=8080

# Comma-separated list of origins allowed to call this API (see WebConfig).
# Defaults to the Create React App dev server.
allowed.origins=${ALLOWED_ORIGINS:http://localhost:3000,http://127.0.0.1:3000}